# 2026-04-29 工作日志

## AntennaDeviceWindow 波控遥测数据功能增强（完成）

### 1. 数字遥测标红
- **文件**: `antennadevicewindow.cpp` → `updateWaveControlTelemetry()`
- **逻辑**: 当 `values[14]`（数字遥测，索引14）不为 `"0x000000"` 时，将该行表格单元格设为红底白字 (`#e74c3c` / `#ffffff`)，否则恢复默认样式。

### 2. 遥测数据 CSV 自动保存
- **文件**: `antennadevicewindow.h` + `.cpp`
- **新增成员变量**: `m_telemetryCsvFile`, `m_telemetryCsvStream`, `m_currentCsvDate`
- **新增方法**:
  - `buildTelemetryColumnNames()`: 返回 181 列列名（时间 + 波控1~12 × 各15项）
  - `ensureTelemetryCsvFileOpen()`: 桌面创建"天线遥测数据"文件夹，按日期打开/创建 CSV 文件，新文件写入 BOM + 表头
  - `saveTelemetryToCsv()`: 追加一行数据（时间 + 180列值），每行181列
- **触发点**: `parseWaveControlTelemetry()` 解析完12路数据后调用 `saveTelemetryToCsv()`
- **文件路径**: 桌面/天线遥测数据/yyyyMMdd_遥测数据.csv
- **特性**: 按日期自动切换文件、追加模式、UTF-8 BOM 头、毫秒级时间戳
