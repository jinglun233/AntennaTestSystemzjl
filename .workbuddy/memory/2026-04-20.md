# 2026-04-20 工作日志

## 1. 温度窗口完整实现（TemperatureInfoWindow）—— commit `e4642ac`

**需求**：设计 temperatureinfowindow 页面，包含：
- 112 路测温通道温度长表（3列：序号/名称/温度）
- 50 路开关状态长表（3列：序号/名称/状态）—— 用户要求合并为同一 Tab 下的两个表格
- QCustomPlot 全通道温度曲线显示
- 测温通道与开关映射配置功能
- 热控指令区：加热回路手动控制、阈值上注、热控模块复位

**实现细节**：
- **UI 布局**：顶部热控指令区 → 中间 TabWidget(测温表+开关表) | 右侧映射配置表 → 底部 QCustomPlot 曲线
- **parseThermalTelemetry()**：解析遥测 payload，112 路 ×1B 温度数据 + 50 路 bit-packed 开关状态
- **QCustomPlot 集成**：动态创建 m_customPlot 嵌入 curveContainer(QWidget)，支持全112路柱状/折线图、暂停/清除、单通道选择过滤、自动 Y 轴范围
- **热控指令帧构造**：加热控制 EB90B2、热控复位 EB90B4（1029B 帧格式）
- **映射配置**：JSON 格式加载/保存/应用，支持每路开关配主/备测温通道

## 3. 1029B 测试数据帧生成
- 重写 `gen_test_frame.py`，生成完整 1029B 回复遥测帧 (EB90 04 C1 + 1024B Payload + Sum8)
- 数据域含模拟数据：112路正弦波形温度(10~245°C) + 50路bit-packed开关状态 + 热控标志0x55
- 输出3种格式：二进制.bin / 纯Hex字符串.txt / C语言字节数组
- 校验和仅对数据域(1024字节)累加取低8位 = 0xE5
- 用于网络调试助手测试服务端 ProtocolCodec::parse() 解析逻辑

## 4. 温度曲线重构 + 开关状态高亮 + QCustomPlot 升级
- **温度曲线**：从单条柱状图改为112路独立时间序列曲线，横轴=时间(秒)，滚动窗口300点
- **开关表格**：开=红色背景(#e74c3c)+白字+加粗；关=恢复交替行色（不再变黑）
- **QCustomPlot升级**：从自写精简版(10KB h/260B cpp)替换为完整版v2.1.1(310KB h/1.3MB cpp)，支持QCPAxisTickerTime等全部API
- **parseThermalTelemetry补实现**：antennadevicewindow.cpp中补上缺失的函数体解决链接错误
- 横坐标使用QCPAxisTickerTime格式化为HH:mm:ss

**修改文件清单**：
| 文件 | 改动 |
|------|------|
| temperatureinfowindow.h | 新增时间序列成员变量(CURVE_MAX_POINTS/m_timeSeries/m_elapsedTimer) |
| temperatureinfowindow.cpp | 重写initCurveContainer/updateCurves，updateSwitchStatus红色高亮，时间格式ticker |
| antennadevicewindow.cpp | 补充parseThermalTelemetry()实现 |
| qcustomplot.h / .cpp | 替换为QCustomPlot v2.1.1完整版 |
| gen_test_frame.py | 重写测试帧生成脚本 |

## 6. 修复温度曲线滚动条配合+温度值不对应问题
- **滚动条bug根因**：initCurveContainer()给curveContainer设了双层layout冲突；m_scrollBarDragging未初始化/未连接sliderPressed/Released；onTimeScrollChanged计算逻辑与updateCurves对不上
- **修复**：统一单层layout；连接拖拽信号跟踪状态；横轴改用相对时间(elapsed秒)避免epoch大数值精度；用blockSignals防valueChanged循环触发
- **温度值bug**：parseThermalTelemetry中 data[3+i]/256.0 错误，原始字节就是0~255℃温度值，改为直接quint8转换
- 额外修复：限制QCustomPlot只允许Y轴拖拽缩放(避免与X轴滚动条冲突)；补连button_pauseCurve/clearCurve槽函数

## 5. GitHub 推送 commit `2a6b8bc`
- 按用户要求**原样上传**所有修改，不修复编译错误
- 推送内容：TemperatureInfoWindow完整实现 + QCustomPlot v2.1.1 + gen_test_frame.py + parseThermalTelemetry补实现
- 已知未修复问题：cpp中使用了 m_timeScrollBar/m_tracer/m_tooltipText/onCurveMouseMoved/onTimeScrollChanged/m_scrollBarDragging 但头文件未声明；温度表/UI列数不匹配(2 vs 3)；映射表列数不匹配(4 vs 5)
- 远程: origin/main, 3a1e30a..2a6b8bc

## 2. GitHub 推送问题（历史）
- 网络不稳定，GitHub HTTPS 连接超时/HTTP2 帧错误
- 已切换 http.version 为 HTTP/1.1 但仍然连接失败
- 本地提交安全，等网络恢复后可 push（本次推送已成功）
