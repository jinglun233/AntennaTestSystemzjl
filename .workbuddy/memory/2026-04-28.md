# 2026-04-28 工作日志

## PowerVoltageWindow 嵌入浏览器（未提交）

**改动**：在电源电压页面嵌入基于 Chromium 的 QWebEngineView 浏览器
- **AntennaTestSystemzjl.pro**：添加 `webenginewidgets` 模块
- **powervoltagewindow.h**：新增 `QWebEngineView *m_browser` 成员
- **powervoltagewindow.ui**：添加 `browserContainer`（native QWidget）+ QVBoxLayout 容器，边距全 0
- **powervoltagewindow.cpp**：构造中创建 QWebEngineView 放入 browserLayout，默认加载 google.com

## ElectronicDeviceWindow 手动修改（用户已完成）
- groupBox 标题已改为"成像指令"
- 用户手动在 groupBox 内添加 SAR 电子设备设置按钮（带宽、脉宽、PRF、发射、接收、工作模式等）
