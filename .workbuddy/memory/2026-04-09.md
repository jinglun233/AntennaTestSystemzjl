# 2026-04-09 工作日志

1. **修复 resources.qrc 编译错误**：rcc 编译器路径解析问题，改用显式 alias 写法 `<file alias="file.svg">resources/file.svg</file>`
2. **修复遥控1/遥控2 遥测表格大小不一致**：
   - 尝试过 Stretch 模式和 UI 文件写数据两种方案
   - 最终方案：三列全部使用 Fixed 固定宽度(序号35/名称100/数值55)，恢复 cpp 中完整数据创建逻辑
   - Qt .ui 文件中的 <row> 数据不会被 uic 加载，必须通过 C++ 代码创建 QTableWidgetItem
3. **UI 文件添加完整表格数据供 Designer 预览**：
   - 用户需求：在 Qt Designer 编辑页面直接看到 12 个表格的完整内容（15行×3列），方便可视化调整间距
   - 为全部12个 QTableWidget 添加了 rowCount=15、完整行数据、属性(editTriggers/selectionBehavior/alternatingRowColors/verticalHeaderVisible)
   - 运行时仍由 cpp setupTelemetryTable() 负责数据创建，UI 数据仅供 Designer 预览用
   - 波控1(telemetryTable1_1) 使用了完整展开格式，其余11个使用紧凑格式减少文件体积
4. **修复 UI 文件 uic 编译错误（第二次）**：
   - 错误：`Unexpected element column` at line 1720
   - 原因：上次修复把 `<item>` 错误换成了 `<column>`，但 `<row>` 内部也不能用 `<column>`
   - 正确格式：`<row>` 内部必须用 **带属性的** `<item row="N" column="M">`
   - 用 Python 脚本批量替换所有行内 `<column>` 为正确格式
   - 验证通过：180 行数据全部转换正确，0 个残留错误
